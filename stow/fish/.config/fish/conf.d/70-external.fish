# Load dircolors
# if test -e "$HOME/.dir_colors/dircolors"
#     # Use -c for csh-style output which fish can parse
#     dircolors -c "$HOME/.dir_colors/dircolors" | string replace -a ' ' \n | source
# end
