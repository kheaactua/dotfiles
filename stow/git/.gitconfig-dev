; Config setup for development.  Tool related config options were moved to
; gitconfig-tools so it's easier to not map them in containers

[includeIf "gitdir:~/workspace/capi/**/"]
  path = ~/.gitconfig-work
[includeIf "gitdir:~/workspace/phx-fsb/**/"]
  path = ~/.gitconfig-work
[includeIf "gitdir:/tmp/phx-launcher*/**/"]
  path = ~/.gitconfig-work
[includeIf "gitdir:/tmp/phx-fsb/**/"]
  path = ~/.gitconfig-work
[includeIf "gitdir:/tmp/manifest/**/"]
  path = ~/.gitconfig-work
[includeIf "gitdir:/tmp/vsomeip-source/**/"]
  path = ~/.gitconfig-work
[includeIf "gitdir:/src/**/"]
  path = ~/.gitconfig-work
[includeIf "gitdir:/f/**/*"]
  path = ~/.gitconfig-work
[includeIf "gitdir:/f/**/"]
  path = 	~/.gitconfig-work
; [includeIf "gitdir:/tmp/**/"]
;   path = 	~/.gitconfig-work
[includeIf "gitdir:~/.conan*/**/"]
  path = 	~/.gitconfig-work
[url "https://github.com"]
  insteadOf = ssh://github.com

[core]
  quotepath = false
  commitGraph = true
  commentchar = @
  ; pager = delta
[gc]
  writeCommitGraph = true
[merge]
  tool = diffview
[color]
  ui = auto
[push]
  default = current
[pull]
  rebase = true
[fetch]
  prune = true
[rebase]
  autoStash = true
  ; autoSquash = true

[diff]
  colorMoved = default

[uploadpack]
  allowFilter = true

[init]
  defaultBranch = main

[merge]
  conflictStyle = diff3

[difftool]
  # Run the difftool immediately, don't ask 'are you sure' each time.
  prompt = false

[protocol]
	allow = always

; vim: ts=2 sw=2 sts=0 noet ft=gitconfig :
